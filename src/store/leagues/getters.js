const getters = {
  selectedLeague: state => state.selectedLeague,
  selectedSeason: state => state.selectedSeason,
  selectedSeasons: state => state.selectedSeasons,
  selectedRace: state => state.selectedRace,
  selectedRaces: state => state.selectedRaces,
  selectedResult: state => state.selectedResult,
  leagues: state => state.leagues
};

export default getters;
