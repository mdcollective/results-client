const getters = {
  results: state => state.results,
  individualResults: state => state.individualResults,
  ageGroupResults: state => state.ageGroupResults,
  teamResults: state => state.teamResults,
  overallIndividualResults: state => state.overallIndividualResults,
  overallTeamResults: state => state.overallTeamResults
};

export default getters;
